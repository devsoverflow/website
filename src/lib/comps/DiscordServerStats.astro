---
import ServerStats from '@/lib/comps/ServerStats.svelte';
import { getServerStats } from '@/lib/server/dsserver';

const stats = await getServerStats(import.meta.env.DISCORD_SERVER_ID);
---

<ServerStats stats={stats} client:load />
